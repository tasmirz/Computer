#include <bits/stdc++.h>

using namespace std;

int main(int argc, char* argv[]) {
  assert(argc == 2);
  ifstream source(argv[1]);
  ofstream compiled(argv[2]);
  /*
  expand complex instructions
  into assembly
  find location of labels remove labels
  use deque
  */
}